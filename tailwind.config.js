/** @type {import('tailwindcss').Config} */
module.exports = {
  // ... other config
  theme: {
    extend: {
      fontFamily: {
        gaegu: ["var(--font-gaegu)"],
      },
    },
  },
  // ... other config
};
